<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Links</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
        rel="stylesheet">
</head>

<body>

    <div class="container">
        <h1>Links</h1>
        <a href="/add">Adicionar Link</a>
        <% for(let i = 0;  i <  links.length; i++){%>

        <div class="card p-4 mb-5" id="<%= links[i]._id %>">

            <form class="d-flex align-items-center justify-content-between" action="/?_method=DELETE" method="post">
                <div class="d-flex  flex-column">
                    <h2><%= links[i].title %></h2>
                    <div><%= links[i].description %> </div>
                    <div class="text-secondary"><%= links[i].url %></div>
                    <div class="text-info"> <i class="material-icons">
                        bar_chart
                    </i>Clicks:<%= links[i].click %></div>
                        
                </div>
                <div class="d-flex flex-column">
                    <input name="id" value='<%= links[i]._id %>' hidden>
                    <a class="btn btn-info mb-2" href="/edit/<%= links[i]._id %>">Edit</a>
                    <button class="btn btn-danger" type="submit">Delete</button>
                  
                </div>
            </form>
        </div>

        <% } %>
    </div>





</body>

</html>